[project]
name = "todo-list-web"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.120.1",
    "redis>=7.0.1",
    "uvicorn>=0.38.0",
]
